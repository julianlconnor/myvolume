myvolu.me

Cron is initially set up to be called automatically by heroku cron.

In order to initially pull data from beatport (there's probably a better way to do this), you have to uncomment the pull functions in the home controller index.
