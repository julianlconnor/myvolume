<!DOCTYPE html>
<html>
<head>
  <title>myvolu.me</title>
  <meta http-equiv="content-type" content="text/html; charset=utf-8" />
  <link rel="shortcut icon" href="/favicon.ico" />
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :all %>
  <%= csrf_meta_tag %>
  <!-- Google Analytics -->
  <script type="text/javascript">
	  var _gaq = _gaq || [];
	  _gaq.push(['_setAccount', 'UA-12298170-2']);
	  _gaq.push(['_trackPageview']);

	  (function() {
	    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
	    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
	    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
	  })();
	</script>
	<!-- End Google Analytics -->
</head>
<body>
	<div id="topbar">
		<div id="logo"><%= link_to "myvolu.meα", root_path  %></div>
    <div id="tagline">An easy way to discover new music.</div>
    <div id="flashSpace">
			<%= render 'layouts/flash' %>
    </div>
		<!-- <ul id="topnav">
			<li class="highlighted"><%= link_to "Charts", charts_path %></li>
		</ul> -->
    <div id="right">
      <span id="usernav">
        <%= render 'home/userNav' %>
      </span>
      <span id="lastupdate"><%= CronWatcher.last.last_update.strftime("Last Updated at %I:%M%p on %m/%d/%Y ♥") %></span>   
    </div>
	</div>
	<div id="container">
		<div id="header">
		</div>
		<%= yield %>
		<div id="footer">
			<div class="content_wrapper">
				<p id="copyright">
				©2011 Julian Connor
				<strong>I hope this helps you. ;)</strong>
				</p>
			</div>
		</div>
	</div>
	


</body>
</html>
<script>
	// $('.close_flash').live('click', function(e) {
	// 		$('#flash').slideToggle("slow");
	// 	});
	// if ($.browser.mozilla) {
	// 	var as = audiojs.createAll();
	//   }
	// else if ($.browser.webkit) {
	// 	var as = audiojs.createAll();
	// }
	// else {
	// 	audiojs.events.ready(function() {
	// 	    var as = audiojs.createAll();
	// 	  });
	// }
	function login() {
	  var email = $('#user_email').val();
	  var password = $('#user_password').val();
	  var data = {remote: true, commit: "Sign in", utf8: "✓",
	              user: {remember_me: 1, password: password, email: email}};
	  $.post('/users/sign_in.json', data, function(resp) {
	    if(resp.success) {
	      
	    } else {
	      
	    }
	  });
	  return false;
	}
	
	
</script>

